SELECT {{Constants.AccountQueryFields.join(", ")}} FROM salesforce.accounts where id in ({{`'${Utils.getAllAccountIds()?.join(`', '`)}'`}}) AND (customer_segmentation_c in ({{CustomerSegment.selectedOptionValues.length > 0 ? `'${CustomerSegment.selectedOptionValues.join(`', '`)}'` : `'${CustomerSegment.options.map(item => item.value).join(`', '`)}'`}}) OR customer_segmentation_c is null) ORDER BY created_date DESC
