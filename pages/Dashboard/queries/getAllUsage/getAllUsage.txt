SELECT stripe_customer_id, SUM(billed_sessions) as total_sessions FROM cloud_services."usage_daily_stats" where stripe_customer_id in (Select stripe_id_c from salesforce.accounts where id in (Select account_id from salesforce.opportunities where stage_name = 'Closed Won')) and date between '{{From_Date.formattedDate}}' AND '{{To_Date.formattedDate}}' GROUP By stripe_customer_id